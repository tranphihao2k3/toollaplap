<UserControl x:Class="LapLapAutoTool.Views.LaptopTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Test chức năng" 
                   FontSize="32" FontWeight="Black" Foreground="White" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="1" 
                   Text="Kiểm tra toàn bộ phần cứng laptop — không cần phần mềm bên ngoài" 
                   FontSize="14" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,28"/>

        <!-- Test Cards Grid -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding TestItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Height="215" Margin="0,0,18,18"
                                Background="{StaticResource SurfaceBrush}"
                                CornerRadius="14"
                                BorderBrush="#334155" BorderThickness="1"
                                Padding="18">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#2563EB"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12" Color="Black" Opacity="0.15" ShadowDepth="3"/>
                            </Border.Effect>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Colored Icon -->
                                <Border Grid.Row="0" Width="46" Height="46" CornerRadius="10"
                                        Background="{Binding AccentColor, Converter={StaticResource ColorToBrushConverter}}"
                                        HorizontalAlignment="Left" Margin="0,0,0,14">
                                    <Path Data="{Binding IconData}"
                                          Fill="White"
                                          Stretch="Uniform"
                                          Width="22" Height="22"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <TextBlock Grid.Row="1" Text="{Binding Title}"
                                           FontSize="16" FontWeight="Bold" Foreground="White" 
                                           Margin="0,0,0,6"/>

                                <TextBlock Grid.Row="2" Text="{Binding Description}"
                                           FontSize="11.5" Foreground="{StaticResource MutedTextBrush}"
                                           TextWrapping="Wrap" LineHeight="17"
                                           VerticalAlignment="Top"/>

                                <Button Grid.Row="3"
                                        Content="Test Ngay →"
                                        Command="{Binding Command}"
                                        Style="{StaticResource ModernButton}"
                                        HorizontalAlignment="Left"
                                        Padding="14,7"
                                        FontSize="12"
                                        Margin="0,10,0,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
