<UserControl x:Class="LapLapAutoTool.Views.StudentAppsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Phần mềm Sinh viên" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
            <TextBlock Text="Các gói phần mềm chuyên dụng và học tập" Foreground="{StaticResource MutedTextBrush}" FontSize="14"/>
        </StackPanel>

        <!-- Content Area -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Professional Apps -->
                <TextBlock Text="THIẾT KẾ &amp; CHUYÊN NGHIỆP" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="10,0,0,10"/>
                <ItemsControl ItemsSource="{Binding ProfessionalApps}" Margin="0,0,0,20">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource ModernCard}" Margin="5" Padding="15,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Bold" Foreground="White"/>
                                        <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{StaticResource MutedTextBrush}"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{Binding Status}" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource AccentBrush}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Academic Apps -->
                <TextBlock Text="HỌC TẬP &amp; VĂN PHÒNG" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="10,0,0,10"/>
                <ItemsControl ItemsSource="{Binding AcademicApps}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource ModernCard}" Margin="5" Padding="15,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Bold" Foreground="White"/>
                                        <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{StaticResource MutedTextBrush}"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{Binding Status}" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource AccentBrush}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Info & Action -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Tổng dung lượng: " Foreground="{StaticResource MutedTextBrush}"/>
                        <TextBlock Text="{Binding TotalSizeMb}" Foreground="White" FontWeight="Bold"/>
                        <TextBlock Text=" MB" Foreground="White" FontWeight="Bold" Margin="0,0,20,0"/>
                        
                        <TextBlock Text="Thời gian dự kiến: " Foreground="{StaticResource MutedTextBrush}"/>
                        <TextBlock Text="{Binding EstimatedTimeMin}" Foreground="White" FontWeight="Bold"/>
                        <TextBlock Text=" phút" Foreground="White" FontWeight="Bold"/>
                    </StackPanel>
                    <ProgressBar Value="{Binding Progress}" Maximum="100" Height="6" Foreground="{StaticResource PrimaryBrush}" Background="#334155" BorderThickness="0"/>
                </StackPanel>
                
                <Button Grid.Column="1" Content="Cài đặt đã chọn" 
                        Command="{Binding StartInstallCommand}" 
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                        Style="{StaticResource ModernButton}" 
                        Padding="30,12" Margin="20,0,0,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
