<UserControl x:Class="LapLapAutoTool.Views.BackupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Sao lưu dữ liệu" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
            <TextBlock Text="Chọn các thư mục quan trọng để sao lưu trước khi cài lại Windows" Foreground="{StaticResource MutedTextBrush}" FontSize="14"/>
        </StackPanel>

        <!-- Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Selection Card -->
            <Border Grid.Column="0" Style="{StaticResource ModernCard}" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="DANH SÁCH THƯ MỤC" FontSize="11" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,15"/>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding BackupFolders}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}" Margin="0,0,0,15">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="10,0,0,0"/>
                                            <TextBlock Text="{Binding Path}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="15,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </CheckBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Action Card -->
            <Border Grid.Column="1" Style="{StaticResource ModernCard}" Margin="10,0,0,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="SẴN SÀNG SAO LƯU" FontSize="11" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,15" HorizontalAlignment="Center"/>
                    
                    <TextBlock Text="Dữ liệu sẽ được nén/mô tạ tại thư mục mới trên Desktop. Vui lòng đảm bảo ổ C còn đủ dung lượng." 
                               Foreground="{StaticResource MutedTextBrush}" FontSize="13" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,30"/>
                    
                    <Button Content="Bắt đầu sao lưu ngay" 
                            Command="{Binding BackupDataCommand}" 
                            Style="{StaticResource ModernButton}" 
                            Height="50" FontSize="16"/>
                    
                    <TextBlock Text="Tự động tạo thư mục Backup_CaiWin_..." 
                               Foreground="{StaticResource MutedTextBrush}" FontSize="11" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="2" Background="#DD000000" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="500">
                <TextBlock Text="{Binding StatusText}" Foreground="White" Margin="0,0,0,20" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                
                <ProgressBar Value="{Binding BackupProgress}" Height="15" Maximum="100" 
                             Style="{StaticResource ModernProgressBar}"
                             Background="#1E293B" Foreground="{StaticResource AccentBrush}"/>
                
                <Grid Margin="0,10,0,0">
                    <TextBlock Text="{Binding BackupProgress, StringFormat={}{0:N0}%}" Foreground="White" FontSize="14" FontWeight="Bold" HorizontalAlignment="Left"/>
                    <TextBlock Text="Vui lòng không tắt ứng dụng..." Foreground="#94A3B8" FontSize="12" HorizontalAlignment="Right"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
