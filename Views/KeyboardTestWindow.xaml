<Window x:Class="LapLapAutoTool.Views.KeyboardTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Test Bàn Phím — Nhấn từng phím để kiểm tra"
        Width="1400" Height="550" 
        WindowStartupLocation="CenterScreen"
        Background="#0F172A"
        ResizeMode="CanMinimize"
        PreviewKeyDown="Window_PreviewKeyDown"
        KeyDown="Window_KeyDown"
        KeyUp="Window_KeyUp"
        Focusable="True">
    <Grid Margin="18,14,18,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12" VerticalAlignment="Center">
            <TextBlock Text="⌨ Test Bàn Phím" FontSize="20" FontWeight="Black" Foreground="White" VerticalAlignment="Center"/>
            <TextBlock Text="  —  Nhấn từng phím, phím sáng xanh = hoạt động tốt" 
                       FontSize="12" Foreground="#64748B" VerticalAlignment="Center" Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Keyboard Canvas (auto-scaled) -->
        <Viewbox Grid.Row="1" Stretch="Uniform" VerticalAlignment="Top" Margin="0,10,0,10">
            <Canvas x:Name="KeyboardCanvas" 
                    Width="1015" Height="270"
                    VerticalAlignment="Top" HorizontalAlignment="Center"/>
        </Viewbox>

        <!-- Status Bar -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#1E293B" CornerRadius="6" Padding="12,6">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Phím: " Foreground="#64748B" FontSize="12" VerticalAlignment="Center"/>
                    <TextBlock x:Name="PressedKeyText" Text="-" 
                               Foreground="#38BDF8" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text="   |   " Foreground="#334155" FontSize="12" VerticalAlignment="Center"/>
                    <TextBlock x:Name="StatusText" Text="Đã test: 0 phím" 
                               Foreground="#10B981" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Button Grid.Column="1" Content="Reset" Click="Reset_Click"
                    Padding="16,6" Style="{StaticResource ModernButton}" FontSize="12"
                    Margin="8,0,0,0"/>
            <Button Grid.Column="2" Content="Đóng" Click="Close_Click"
                    Padding="16,6" Style="{StaticResource SecondaryButton}" FontSize="12"
                    Margin="8,0,0,0"/>
        </Grid>
    </Grid>
</Window>
