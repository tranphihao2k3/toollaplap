<Window x:Class="LapLapAutoTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:LapLapAutoTool.ViewModels"
        xmlns:views="clr-namespace:LapLapAutoTool.Views"
        mc:Ignorable="d"
        Title="Bộ Công Cụ Kỹ Thuật Viên LapLap" 
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Window.DataContext>
        <viewmodels:MainWindowViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- DataTemplates to link ViewModels to Views -->
        <DataTemplate DataType="{x:Type viewmodels:HardwareViewModel}">
            <views:HardwareView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:QuickSetupViewModel}">
            <views:QuickSetupView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:StudentAppsViewModel}">
            <views:StudentAppsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:UtilitiesViewModel}">
            <views:UtilitiesView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" Padding="15,40,15,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,50">
                    <TextBlock Text="LAPLAP" FontSize="26" FontWeight="Black" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="AUTO TOOL" FontSize="11" FontWeight="Bold" Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" Margin="0,-5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <RadioButton Style="{StaticResource SidebarButton}" Content="Thông tin phần cứng" Command="{Binding ShowHardwareCommand}" IsChecked="True"/>
                    <RadioButton Style="{StaticResource SidebarButton}" Content="Cài đặt nhanh" Command="{Binding ShowQuickSetupCommand}"/>
                    <RadioButton Style="{StaticResource SidebarButton}" Content="Phần mềm sinh viên" Command="{Binding ShowStudentAppsCommand}"/>
                    <RadioButton Style="{StaticResource SidebarButton}" Content="Công cụ hệ thống" Command="{Binding ShowUtilitiesCommand}"/>
                </StackPanel>

                <StackPanel Grid.Row="2">
                    <Separator Background="#334155" Margin="0,10"/>
                    
                    <Button Content="{Binding MachineCode}" 
                            Command="{Binding CopyHWIDCommand}" 
                            Style="{StaticResource SidebarHWIDButton}" 
                            ToolTip="Mã máy HWID - Click để Copy"/>

                    <TextBlock Text="Chế độ Quản trị viên" Foreground="#10B981" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <ContentControl Content="{Binding CurrentView}" Grid.Row="0"/>
            
            <!-- Bottom Status Bar -->
            <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" BorderBrush="#334155" BorderThickness="0,1,0,0" Padding="15,8">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Trạng thái hệ thống: " Foreground="{StaticResource MutedTextBrush}" FontSize="11"/>
                        <TextBlock Text="Sẵn sàng" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                        <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="10,0" Background="#334155"/>
                        <TextBlock Text="Bảo vệ: Bình thường" Foreground="{StaticResource MutedTextBrush}" FontSize="11"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="{Binding LicenseStatus}" 
                                Command="{Binding ActivateLicenseCommand}"
                                Foreground="{StaticResource AccentBrush}" 
                                FontSize="11" FontWeight="Bold" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,15,0"/>
                        <TextBlock Text="v1.0.0" Foreground="{StaticResource MutedTextBrush}" FontSize="11"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
